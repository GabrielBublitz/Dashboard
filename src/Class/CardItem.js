class CardItem{

    constructor(url, server, index, alertLog){
        this.url = url,
        this.server = server,
        this.index = index,
        this.alertLog = alertLog 
    }

    url
    server
    index
    alertLog
}

export default CardItem;