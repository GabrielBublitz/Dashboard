class CollapsedCardItem {
    constructor(title, cardClass, visual) {
        this.data = {
            title: title,
            cardClass: cardClass,
            visual: visual
        };
    }
}

export default CollapsedCardItem;