class CollapsedCardItem {
    constructor(title, firstUrl, secondUrl, visual) {
        this.data = {
            title: title,
            firstUrl: firstUrl,
            secondUrl: secondUrl,
            visual: visual
        };
    }
}

export default CollapsedCardItem;